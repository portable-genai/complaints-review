"""``live`` profile adapters: the shared local model on the laptop stack.

Under live every port binds the SDK-free ``local`` adapter except the model port, which
calls the fleet's local open-weight model through :mod:`hex_service_kit.localmodel`. Every
consequential figure (deadlines, conduct flags from rules) stays deterministic; the model
summarises, categorises and drafts, and a human reviews every draft.
"""
